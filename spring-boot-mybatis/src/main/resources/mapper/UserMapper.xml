<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.warriorg.spring.mybatis.mapper.UserDao">

    <select id="findOne" parameterType="int" resultType="me.warriorg.spring.mybatis.model.User">
        SELECT * FROM user WHERE sid = #{sid}
    </select>
</mapper>